import React from 'react';
import useStyles from "../../../style";

const Post = () => {
    const classes = useStyles();
    return (
        <div>
post
        </div>
    );
};

export default Post;
